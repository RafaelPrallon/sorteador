<%= form_for(@sorteado) do |f| %>
  <% if @sorteado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sorteado.errors.count, "error") %> prohibited this sorteado from being saved:</h2>

      <ul>
      <% @sorteado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
